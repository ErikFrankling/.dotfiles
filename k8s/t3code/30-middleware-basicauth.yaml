apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: t3code-basic-auth
  namespace: t3code
spec:
  basicAuth:
    secret: t3code-basic-auth      # admin + password (your password manager)
    removeHeader: true             # strip the Basic creds before upstream
    realm: "T3 Code"
