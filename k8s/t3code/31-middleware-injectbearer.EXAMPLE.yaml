# Applied imperatively so the bearer stays out of git. See apply command in README.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: t3code-inject-bearer
  namespace: t3code
spec:
  headers:
    customRequestHeaders:
      Authorization: "Bearer <T3_BEARER_TOKEN>"   # t3 auth session issue
